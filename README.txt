This project is intended to list the CSV file of NBA teams. 
The project will not work unless the user provides a type of database that can 
read the CSV file. An example would be using a WAMP or MAMP server. 
Thus the connection string and password string can vary when connecting to the database. 